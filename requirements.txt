# ── Vercel deployment requirements (Flask app boot + frontend) ────────────────
# Kept intentionally minimal. Vercel serverless functions have a 250 MB unzipped
# size limit and no system tkinter, so the heavy desktop / scraping / plotting
# dependencies live in requirements-full.txt (used by Render and local dev).
#
# The app imports mongo_loader (-> pymongo) at boot, so pymongo is required even
# for a frontend-only deploy; it is only the driver and does not connect on import.
flask>=3.0.0
pymongo>=4.6.0
